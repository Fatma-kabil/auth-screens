import 'package:flutter/material.dart';

double width(context) => MediaQuery.of(context).size.width;
double height(context) => MediaQuery.of(context).size.height;

const Color maincolor = Color(0xff64a5ff);
